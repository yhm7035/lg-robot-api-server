import Redis from 'redis';
import { RedisCallback } from '../common/types';
export default class RedisClient {
    private callbackTable;
    private client;
    private onSub;
    private onceSub;
    constructor(options?: Redis.ClientOpts);
    unref(): void;
    getClient(): Redis.RedisClient;
    on(pattern: string, callback: RedisCallback): void;
    off(pattern: string): void;
    once(key: string, value: string | object, pattern: string): Promise<any>;
    keys(pattern: string): Promise<any[]>;
    get(key: string): Promise<any>;
    set(key: string, value: object | string): Promise<unknown>;
    del(key: string): Promise<unknown>;
}
