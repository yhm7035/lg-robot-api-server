import * as ainUtil from '@ainblockchain/ain-util';
import AinJS from '@ainblockchain/ain-js';
import { EnvType } from '../common/types';
export default class Wallet {
    private wallet;
    private mnemonic;
    private secretKey;
    private address;
    private ainJs;
    constructor(mnemonic: string, type: EnvType);
    getWallet(): any;
    getMnemonic(): string;
    getSecretKey(): string;
    getAddress(): string;
    getAinJs(): AinJS;
    signaturePayload(payload: object): {
        payload: object;
        signature: string;
        fields: ainUtil.Field[];
        address: string;
    };
}
